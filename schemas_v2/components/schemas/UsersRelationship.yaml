description: Relationship to users.
properties:
  data:
    description: Relationships to user objects.
    example: []
    items:
      $ref: ./UserRelationshipData.yaml
    type: array
required:
  - data
type: object
