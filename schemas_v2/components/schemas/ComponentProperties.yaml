additionalProperties: {}
description: >-
  Properties of a UI component. Different component types can have their own
  additional unique properties. See the [components
  documentation](https://docs.datadoghq.com/service_management/app_builder/components/)
  for more detail on each component type and its properties.
properties:
  children:
    description: The child components of the UI component.
    items:
      $ref: ./Component.yaml
    type: array
  isVisible:
    $ref: ./ComponentPropertiesIsVisible.yaml
type: object
