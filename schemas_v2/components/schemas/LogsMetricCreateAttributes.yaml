description: The object describing the Datadog log-based metric to create.
properties:
  compute:
    $ref: ./LogsMetricCompute.yaml
  filter:
    $ref: ./LogsMetricFilter.yaml
  group_by:
    description: The rules for the group by.
    items:
      $ref: ./LogsMetricGroupBy.yaml
    type: array
required:
  - compute
type: object
