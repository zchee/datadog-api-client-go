description: The dimension by which to sort a query's results.
properties:
  aggregation:
    $ref: ./EventsAggregation.yaml
  metric:
    description: >-
      The metric's calculated value which should be used to define the sort
      order of a query's results.
    example: '@duration'
    type: string
  order:
    $ref: ./QuerySortOrder.yaml
  type:
    $ref: ./EventsSortType.yaml
required:
  - aggregation
type: object
