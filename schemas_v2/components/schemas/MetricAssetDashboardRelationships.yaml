description: >-
  An object containing the list of dashboards that can be referenced in the
  `included` data.
properties:
  data:
    description: A list of dashboards that can be referenced in the `included` data.
    items:
      $ref: ./MetricAssetDashboardRelationship.yaml
    type: array
type: object
