description: >-
  Response object with all events matching the request and pagination
  information.
properties:
  data:
    description: Array of events matching the request.
    items:
      $ref: ./AuditLogsEvent.yaml
    type: array
  links:
    $ref: ./AuditLogsResponseLinks.yaml
  meta:
    $ref: ./AuditLogsResponseMetadata.yaml
type: object
