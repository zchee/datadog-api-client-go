description: >-
  The list of RUM retention filter IDs along with their corresponding type to
  reorder.

  All retention filter IDs should be included in the list created for a RUM
  application.
properties:
  data:
    description: A list of RUM retention filter IDs along with type.
    items:
      $ref: ./RumRetentionFiltersOrderData.yaml
    type: array
type: object
