description: >-
  The rum-based metric filter. Events matching this filter will be aggregated in
  this metric.
properties:
  query:
    default: '*'
    description: The search query - following the RUM search syntax.
    example: '@service:web-ui: '
    type: string
required:
  - query
type: object
