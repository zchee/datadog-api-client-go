description: A monitor notification rule.
properties:
  data:
    $ref: ./MonitorNotificationRuleData.yaml
  included:
    description: >-
      Array of objects related to the monitor notification rule that the user
      requested.
    items:
      $ref: ./MonitorNotificationRuleResponseIncludedItem.yaml
    type: array
type: object
