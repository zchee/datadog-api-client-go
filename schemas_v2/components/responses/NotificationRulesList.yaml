content:
  application/json:
    schema:
      properties:
        data:
          items:
            $ref: ../schemas/NotificationRule.yaml
          type: array
      type: object
description: The list of notification rules.
