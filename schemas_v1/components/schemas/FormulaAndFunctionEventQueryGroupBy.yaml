description: List of objects used to group by.
properties:
  facet:
    description: Event facet.
    example: status.
    type: string
  limit:
    description: Number of groups to return.
    example: 10
    format: int64
    type: integer
  sort:
    $ref: ./FormulaAndFunctionEventQueryGroupBySort.yaml
required:
  - facet
type: object
