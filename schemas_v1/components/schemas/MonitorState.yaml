description: Wrapper object with the different monitor states.
properties:
  groups:
    additionalProperties:
      $ref: ./MonitorStateGroup.yaml
    description: >-
      Dictionary where the keys are groups (comma separated lists of tags) and
      the values are

      the list of groups your monitor is broken down on.
    type: object
readOnly: true
type: object
