description: >-
  Response containing the audit logs usage for each hour for a given
  organization.
properties:
  usage:
    description: Get hourly usage for audit logs.
    items:
      $ref: ./UsageAuditLogsHour.yaml
    type: array
type: object
