description: >-
  Response containing the Cloud Workload Security usage for each hour for a
  given organization.
properties:
  usage:
    description: Get hourly usage for Cloud Workload Security.
    items:
      $ref: ./UsageCWSHour.yaml
    type: array
type: object
