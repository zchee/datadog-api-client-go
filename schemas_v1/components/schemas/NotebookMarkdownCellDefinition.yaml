description: >-
  Text in a notebook is formatted with
  [Markdown](https://daringfireball.net/projects/markdown/), which enables the
  use of headings, subheadings, links, images, lists, and code blocks.
properties:
  text:
    description: The markdown content.
    example: |-
      # Example Header 
      example content
    type: string
  type:
    $ref: ./NotebookMarkdownCellDefinitionType.yaml
required:
  - type
  - text
type: object
