description: Cloud Workload Security usage for a given organization for a given hour.
properties:
  cws_container_count:
    description: >-
      The total number of Cloud Workload Security container hours from the start
      of the given hour’s month until the given hour.
    format: int64
    nullable: true
    type: integer
  cws_host_count:
    description: >-
      The total number of Cloud Workload Security host hours from the start of
      the given hour’s month until the given hour.
    format: int64
    nullable: true
    type: integer
  hour:
    description: The hour for the usage.
    format: date-time
    type: string
  org_name:
    description: The organization name.
    type: string
  public_id:
    description: The organization public ID.
    type: string
type: object
