description: >-
  The access role of the user. Options are **st** (standard user), **adm**
  (admin user), or **ro** (read-only user).
enum:
  - st
  - adm
  - ro
  - ERROR
example: ro
nullable: true
type: string
x-enum-varnames:
  - STANDARD
  - ADMIN
  - READ_ONLY
  - ERROR
