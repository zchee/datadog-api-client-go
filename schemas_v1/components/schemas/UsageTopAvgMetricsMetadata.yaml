description: The object containing document metadata.
properties:
  day:
    description: >-
      The day value from the user request that contains the returned usage data.
      (If day was used the request)
    format: date-time
    type: string
  month:
    description: >-
      The month value from the user request that contains the returned usage
      data. (If month was used the request)
    format: date-time
    type: string
  pagination:
    $ref: ./UsageTopAvgMetricsPagination.yaml
type: object
