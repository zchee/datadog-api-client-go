description: >-
  Updating a notebook can either insert new cell(s) or update existing cell(s)
  by including the cell `id`.

  To delete existing cell(s), simply omit it from the list of cells.
oneOf:
  - $ref: ./NotebookCellCreateRequest.yaml
  - $ref: ./NotebookCellUpdateRequest.yaml
