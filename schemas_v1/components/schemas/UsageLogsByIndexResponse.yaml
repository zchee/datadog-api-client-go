description: >-
  Response containing the number of indexed logs for each hour and index for a
  given organization.
properties:
  usage:
    description: An array of objects regarding hourly usage of logs by index response.
    items:
      $ref: ./UsageLogsByIndexHour.yaml
    type: array
type: object
