description: >-
  Response containing the number of profiled hosts for each hour for a given
  organization.
properties:
  usage:
    description: Get hourly usage for profiled hosts.
    items:
      $ref: ./UsageProfilingHour.yaml
    type: array
type: object
