description: Response containing the IoT usage for each hour for a given organization.
properties:
  usage:
    description: Get hourly usage for IoT.
    items:
      $ref: ./UsageIoTHour.yaml
    type: array
type: object
